{
    "builds": [{
        "src": "djangotest/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "djangotest/wsgi.py"
        }
    ]
}